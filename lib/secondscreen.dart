import 'package:flutter/material.dart';

class secondscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('feeds saved will be shown here',
            style: TextStyle(fontSize: 32.0),
          )
      ),
    );
  }
}